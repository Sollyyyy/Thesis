version: "3.8"

services:
  backend:
    build: ./backend
    container_name: tripplanner-backend
    network_mode: host
    volumes:
      - ./backend/users.db:/app/users.db

  frontend:
    build: ./frontend
    container_name: tripplanner-frontend
    network_mode: host
    depends_on:
      - backend
